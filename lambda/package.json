{
  "name": "mif-faq-chat-free-lambda",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "sam build",
    "local:api": "sam local start-api --config-env local",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/*.test.mjs\"",
    "eval:mock": "node eval/faq-chat/run-eval.mjs --mode mock --runs 1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@aws-crypto/sha256-js": "^5.2.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-sts": "^3.971.0",
    "@aws-sdk/credential-provider-node": "^3.971.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@smithy/signature-v4": "^5.4.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.0",
    "yaml": "^2.5.1"
  }
}
